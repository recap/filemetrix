[default]

api_prefix = "/api/v1"
expose_port = 1966
build_date = "unknown"
filemetrix_service_api_key = "changeme"

# Database
db_user = "fms"
db_password = "change_me_password"
db_host = "postgres"
db_port = 5432
db_name = "filemetrix"

# Email / SMTP (optional)
mail_host = "maildev"
mail_port = 1025
mail_usr = ""
mail_pass = ""
mail_from = "e.indarto@gmail.com"
mail_to = ["eko.indarto@dans.knaw.nl"]
mail_use_tls = true
mail_use_ssl = false
mail_use_auth = false

# Logging
log_level = 10
log_file = "./logs/fms.log"
log_format = '%(asctime)s %(levelname)s %(name)s %(threadName)s : %(message)s'

# Other
otlp_enable = false
pid_fetcher_url = "http://pidfetcherservice:1928/"

# Add provider-specific keys below as needed
